import styled from '@emotion/styled';

const WrapperEditor = styled.section((props) => ({
	backgroundColor: 'var(--clr-dark-650)',
	gridArea: 'editor-content',
	height: '100vh',
}));

export default WrapperEditor;
